# 👥 واجهة إدارة الموظفين والراتب

## 🎉 الجديد: تم إضافة بيانات التوظيف والراتب إلى صفحات الموظفين

### ✨ المميزات الجديدة

#### 1️⃣ **صفحة قائمة الموظفين (Index)**

الآن تعرض معلومات إضافية على بطاقة كل موظف:

- 💼 **المسمى الوظيفي** - يظهر تحت الاسم بلون أصفر
- 💰 **الراتب** - يظهر باللون الأخضر مع العملة
- 📋 **نوع التوظيف** - يظهر كـ badge أزرق (دوام كامل، جزئي، عقد، مؤقت)
- 📊 **القسم** - يظهر باللون الرمادي الفاتح
- 📅 **تاريخ التعيين** - يظهر بتنسيق yyyy-mm-dd

```blade
<!-- مثال على البطاقة الجديدة -->
┌────────────────────────────────────┐
│ أحمد محمود              [موظف]    │
│ مهندس برمجيات                      │
│ 📧 ahmad@example.com               │
│ 📞 +964 123 456 7890              │
├────────────────────────────────────┤
│ 💰 1,500,000 IQD                   │
│ 📋 [دوام كامل]                    │
│ 📊 تطوير التطبيقات                 │
│ 📅 2023-01-15                      │
├────────────────────────────────────┤
│ [👁️]  [✏️]  [🗑️]                 │
└────────────────────────────────────┘
```

#### 2️⃣ **صفحة عرض الموظف (Show)**

تم تنظيم المعلومات في قسمين:

**القسم الأول: البيانات الشخصية** 👤

- الاسم
- البريد الإلكتروني
- الهاتف
- التلكرام
- تاريخ الميلاد
- الجنس
- الرقم الوطني
- العنوان

**القسم الثاني: بيانات التوظيف والراتب** 💼 (جديد)

- 💰 الراتب (بتنسيق عملة)
- 📊 القسم
- 🎯 المسمى الوظيفي
- 📋 نوع التوظيف (badge)
- 📅 تاريخ التعيين

#### 3️⃣ **صفحة تعديل الموظف (Edit)**

تم إضافة قسم جديد "💼 بيانات التوظيف والراتب" يحتوي على:

- **الراتب** - حقل رقمي مع خطوات عشرية
- **العملة** - حقل نصي (افتراضي: IQD)
- **القسم** - حقل نصي حر
- **المسمى الوظيفي** - حقل نصي حر
- **نوع التوظيف** - قائمة منسدلة بـ 4 خيارات:
    - دوام كامل
    - دوام جزئي
    - عقد
    - مؤقت
- **تاريخ التعيين** - حقل تاريخ

#### 4️⃣ **صفحة إنشاء موظف جديد (Create)**

نفس القسم الجديد مع نفس الحقول الموجودة في صفحة التعديل.

---

## 📝 التحديثات التقنية

### 1. User Model (`app/Models/User.php`)

الحقول موجودة بالفعل في fillable و casts:

```php
protected $fillable = [
    // ... الحقول الموجودة ...
    'salary',
    'salary_currency',
    'employment_type',
    'department',
    'position',
    'hire_date',
];

protected $casts = [
    'salary' => 'decimal:2',
    'hire_date' => 'date',
    // ... الحقول الأخرى ...
];
```

### 2. UserController (`app/Http/Controllers/Admin/UserController.php`)

تم تحديث دوال store و update لقبول الحقول الجديدة:

**في دالة store():**

```php
$request->validate([
    'salary' => 'nullable|numeric|min:0',
    'salary_currency' => 'nullable|string|max:10',
    'employment_type' => 'nullable|in:full-time,part-time,contract,temporary',
    'department' => 'nullable|string|max:255',
    'position' => 'nullable|string|max:255',
    'hire_date' => 'nullable|date',
]);

$user = User::create([
    // ... الحقول الموجودة ...
    'salary' => $request->salary,
    'salary_currency' => $request->salary_currency ?? 'IQD',
    'employment_type' => $request->employment_type,
    'department' => $request->department,
    'position' => $request->position,
    'hire_date' => $request->hire_date,
]);
```

**في دالة update():**

```php
$user->update($request->only([
    // ... الحقول الموجودة ...
    'salary',
    'salary_currency',
    'employment_type',
    'department',
    'position',
    'hire_date',
]));
```

### 3. Views (التحديثات)

#### `resources/views/admin/users/index.blade.php`

- إضافة قسم "Employment Info" يظهر فقط إذا كانت هناك بيانات
- عرض الراتب بتنسيق الأرقام + العملة
- عرض نوع التوظيف كـ badge
- عرض القسم والتاريخ

#### `resources/views/admin/users/show.blade.php`

- إضافة قسم "💼 بيانات التوظيف والراتب"
- عرض الراتب بتنسيق عملة (مثل 1,500,000.00)
- عرض جميع الحقول الجديدة مع أيقونات واضحة

#### `resources/views/admin/users/edit.blade.php`

- إضافة قسم جديد مع 6 حقول للتعديل
- كل حقل مع تعليقات (placeholders) مفيدة

#### `resources/views/admin/users/create.blade.php`

- إضافة نفس قسم 6 الحقول مع استخدام `old()` helper للقيم المحفوظة

---

## 🔄 مسار العمل الكامل

### إضافة موظف جديد مع بيانات كاملة:

```
1. اضغط "إضافة مستخدم"
2. ملأ البيانات الشخصية (الاسم، البريد، كلمة المرور)
3. ملأ البيانات الإضافية (هاتف، تلكرام، إلخ)
4. ملأ بيانات التوظيف والراتب
   └─ الراتب (مثل: 1500000)
   └─ العملة (مثل: IQD)
   └─ القسم (مثل: تطوير التطبيقات)
   └─ المسمى (مثل: مهندس برمجيات)
   └─ نوع التوظيف (اختر من القائمة)
   └─ تاريخ التعيين (2023-01-15)
5. اختر الدور (موظف، مدير، إلخ)
6. اضغط "إضافة"
```

### عرض بيانات الموظف:

```
1. قائمة الموظفين → تظهر البطاقات مع البيانات الأساسية
2. اضغط العين (👁️) → صفحة التفاصيل الكاملة
3. اضغط التعديل (✏️) → صفحة التعديل
```

### تعديل بيانات الموظف:

```
1. من قائمة الموظفين → اضغط ✏️
2. عدّل أي حقل (الراتب، القسم، المسمى، إلخ)
3. اضغط "تحديث"
```

---

## ✅ قائمة المميزات

| المميزة                      | الحالة | الملاحظات            |
| ---------------------------- | ------ | -------------------- |
| عرض الراتب في قائمة الموظفين | ✅     | بتنسيق عملة          |
| عرض المسمى الوظيفي           | ✅     | تحت الاسم            |
| عرض نوع التوظيف              | ✅     | كـ badge ملون        |
| عرض القسم                    | ✅     | باللون الرمادي       |
| عرض تاريخ التعيين            | ✅     | بصيغة yyyy-mm-dd     |
| صفحة عرض كاملة               | ✅     | مع قسمين منفصلين     |
| تعديل الراتب                 | ✅     | مع التحقق من النوع   |
| تعديل جميع البيانات          | ✅     | مع fallback إلى null |
| إضافة موظف مع بيانات         | ✅     | مع الحفاظ على old()  |
| البحث والفرز                 | ✅     | لم يتأثر بالتحديثات  |

---

## 🎨 التنسيق البصري

### الألوان المستخدمة:

- **💚 أخضر** (success) - الراتب
- **🔵 أزرق** (primary) - الدور ونوع التوظيف
- **💛 أصفر** (warning) - المسمى الوظيفي
- **🔷 أزرق فاتح** (info) - عناوين الأقسام
- **⚫ رمادي** (muted) - البيانات الإضافية

### Responsive Design:

- تظهر البطاقات في عمود واحد على الهاتف
- عمودان على الأجهزة الصغيرة (md)
- ثلاثة أعمدة على الأجهزة الكبيرة (xl)

---

## 🔗 الملفات المعدلة

```
✅ resources/views/admin/users/index.blade.php
   ├─ إضافة قسم Employment Info
   └─ عرض الراتب، المسمى، القسم، التاريخ

✅ resources/views/admin/users/show.blade.php
   ├─ إضافة عنوان "البيانات الشخصية"
   └─ إضافة قسم "بيانات التوظيف والراتب"

✅ resources/views/admin/users/edit.blade.php
   ├─ إضافة قسم جديد
   └─ 6 حقول للتعديل

✅ resources/views/admin/users/create.blade.php
   ├─ إضافة قسم جديد
   └─ 6 حقول مع old()

✅ app/Http/Controllers/Admin/UserController.php
   ├─ تحديث دالة store()
   └─ تحديث دالة update()
```

---

## 📲 الحقول الجديدة الكاملة

| الحقل           | النوع     | الإجباري | الافتراضي | الملاحظات                                 |
| --------------- | --------- | -------- | --------- | ----------------------------------------- |
| salary          | decimal:2 | ✗        | null      | قيمة عددية                                |
| salary_currency | string    | ✗        | IQD       | مثل: IQD, USD, EUR                        |
| employment_type | enum      | ✗        | null      | full-time, part-time, contract, temporary |
| department      | string    | ✗        | null      | اسم القسم                                 |
| position        | string    | ✗        | null      | المسمى الوظيفي                            |
| hire_date       | date      | ✗        | null      | YYYY-MM-DD                                |

---

## 🚀 الخطوات التالية

1. **زيارة صفحة الموظفين:**

    ```
    http://localhost:8000/admin/users
    ```

2. **إضافة موظف جديد مع بيانات:**
    - اضغط "إضافة مستخدم"
    - املأ جميع الحقول
    - اختر نوع التوظيف والراتب
    - اضغط "إضافة"

3. **التحقق من البيانات:**
    - يجب أن تظهر البطاقة مع الراتب والمسمى
    - اضغط العين لرؤية التفاصيل الكاملة

4. **التعديل:**
    - اضغط ✏️
    - عدّل الراتب والبيانات
    - اضغط "تحديث"

---

**تم التحديث: 2026-01-25** ✨

هذا التحديث متوافق تماماً مع نظام الحضور والراتب الموجود في `/admin/attendance`
