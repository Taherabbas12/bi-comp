# 📊 نظام إدارة الحضور والرواتب المتكامل

## ✅ الميزات المضافة

### 1️⃣ **حقول الراتب والتوظيف** 💼

تم إضافة الحقول التالية لجدول المستخدمين:

- **salary** - الراتب الشهري (decimal)
- **salary_currency** - عملة الراتب (افتراضي: IQD)
- **employment_type** - نوع التوظيف (full-time, part-time, contract, temporary)
- **department** - القسم/الإدارة
- **position** - المسمى الوظيفي
- **hire_date** - تاريخ التعيين

### 2️⃣ **جدول أوقات العمل** 🕐

جدول جديد `work_schedules` يحتوي على:

- **day_of_week** - يوم الأسبوع (1-7)
- **official_check_in** - وقت الدخول الرسمي
- **official_check_out** - وقت الخروج الرسمي
- **working_hours** - ساعات العمل اليومية (افتراضي: 8)
- **is_day_off** - هل اليوم عطلة؟

### 3️⃣ **إحصائيات الحضور والغياب** 📊

تم تحسين واجهة الإدارة لعرض:

- ✔️ عدد أيام الحضور
- ❌ عدد أيام الغياب
- ⏱️ إجمالي ساعات الحضور (بصيغة Xh Ym)
- 💼 معلومات الموظف (الراتب، القسم، المسمى الوظيفي)
- 🕐 أوقات العمل الرسمية اليومية

### 4️⃣ **إحصائيات أسبوعية** 📈

لكل أسبوع يتم عرض:

- عدد أيام الحضور والغياب
- إجمالي الساعات العاملة
- حالة الموظف (حاضر/غائب)

---

## 🗂️ الملفات المضافة والمعدلة

### Migrations

```
database/migrations/2026_01_25_000001_add_employment_fields_to_users_table.php
database/migrations/2026_01_25_000002_create_work_schedules_table.php
```

### Models

```
app/Models/WorkSchedule.php (جديد)
app/Models/User.php (معدّل - إضافة علاقات)
```

### Controllers

```
app/Http/Controllers/Admin/AttendanceAdminController.php (معدّل - إضافة حساب الغياب)
```

### Views

```
resources/views/admin/attendance/index.blade.php (معدّل - تحسين العرض)
```

### Seeders

```
database/seeders/WorkScheduleSeeder.php (جديد)
```

---

## 🚀 خطوات التثبيت

### 1. تشغيل الـ Migrations

```bash
php artisan migrate
```

### 2. تشغيل الـ Seeder (لإضافة جداول عمل افتراضية)

```bash
php artisan db:seed --class=WorkScheduleSeeder
```

### 3. إضافة بيانات الموظفين

يمكن إضافة معلومات الراتب والتوظيف عند إنشاء الموظف أو تحديثه.

---

## 📋 مثال على البيانات

### Employee Info

```
الاسم: أحمد محمود
المسمى الوظيفي: مهندس البرمجيات
القسم: تطوير التطبيقات
نوع التوظيف: دوام كامل
الراتب: 1,500,000 IQD
```

### Weekly Statistics

```
الأسبوع 1:
├── أيام الحضور: 4
├── أيام الغياب: 1
├── ساعات العمل: 32h
└── الحالة: ✔ حاضر
```

### Official Working Times

```
الاثنين:    08:00 - 17:00 (8h) - دوام
الثلاثاء:   08:00 - 17:00 (8h) - دوام
الأربعاء:   08:00 - 17:00 (8h) - دوام
الخميس:     08:00 - 17:00 (8h) - دوام
الجمعة:     08:00 - 17:00 (8h) - دوام
السبت:      - (عطلة)
الأحد:      - (عطلة)
```

---

## 🔄 العلاقات في النموذج

### User Model

```php
$user->workSchedules()   // جداول العمل
$user->attendances()     // سجلات الحضور
```

### WorkSchedule Model

```php
$schedule->user()        // الموظف المرتبط
```

---

## 📊 الحسابات المستخدمة

### حساب الغياب

```
أيام الغياب = أيام العمل الرسمية - أيام الحضور الفعلية
```

### حساب الساعات

```
إجمالي الساعات = مجموع (check_out_at - check_in_at)
                ÷ 60 = عدد الساعات
                % 60 = الدقائق المتبقية
```

---

## 🎨 الواجهة الجديدة

### إحصائيات الموظف الشهرية

- بطاقات ملونة تعرض أيام الحضور والغياب والساعات
- معلومات الموظف والراتب
- أوقات العمل الرسمية

### إحصائيات أسبوعية

- بطاقات منفصلة لكل أسبوع
- عرض الحضور والغياب والساعات
- ألوان ديناميكية (أخضر = حاضر، أحمر = غائب)

### جدول الحضور اليومي

- عرض التفاصيل الكاملة لكل يوم
- الأوقات الفعلية والمدة العاملة
- إمكانية التعديل على السجلات

---

## ⚙️ الإعدادات والتخصيص

### تغيير أوقات العمل الرسمية

يمكن تعديل `work_schedules` مباشرة من قاعدة البيانات أو إنشاء واجهة إدارة.

### دعم أنواع توظيف مختلفة

- **full-time**: دوام كامل
- **part-time**: دوام جزئي
- **contract**: عقد
- **temporary**: موظف مؤقت

---

## 🔐 أذونات المراجعة

تأكد من أن الموظف الإداري لديه صلاحية `view_attendance` للوصول إلى الصفحة.

---

## 📝 ملاحظات مهمة

1. **الجدول الافتراضي**: يتم إنشاء جدول عمل افتراضي (أحد إلى الجمعة، 8 ساعات)
2. **حساب الغياب**: يتم حسابه بناءً على أيام العمل الرسمية
3. **الأوقات الرسمية**: يمكن تعديلها لكل موظف على حدة
4. **العملات**: تدعم أي عملة (IQD, USD, EUR, إلخ)

---

## 🐛 استكشاف الأخطاء

إذا واجهت مشكلة:

1. **لم تظهر جداول العمل**: تأكد من تشغيل الـ seeder

    ```bash
    php artisan db:seed --class=WorkScheduleSeeder
    ```

2. **خطأ في الحقول**: تأكد من تشغيل الـ migrations

    ```bash
    php artisan migrate
    ```

3. **بيانات غير دقيقة**: امسح الـ cache
    ```bash
    php artisan cache:clear
    ```

---

تم التطوير في: **2026-01-25**
الإصدار: **1.0.0**
